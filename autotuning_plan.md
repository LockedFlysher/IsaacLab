# 自动化调参与界面开发清单

- [ ] 研读控制器与配置：查看 `isaaclab_nhb/isaaclab_nhb/envs/mpc_env/controllers/mpx/examples/biped/config_hector_wb.py` 中可调参数；阅读 `isaaclab_nhb/isaaclab_nhb/envs/mpc_env/controllers/mpx/examples/biped/multi_env_wb_hector.py` 了解 base 高度目标设置、重置与步进流程。
- [ ] 明确可调参数与约束：确定要扫的增益/权重/初始高度偏置及其上下限与步长；设定正弦轨迹规格（±0.05 m 幅值，频率/相位，单次试验时长）。
- [ ] 增加成功/失败判据：实现 base 倾斜超过 20° 判失败；记录 base 高度 vs 目标的 RMSE 与完成时间。
- [ ] 生成正弦参考：实现动态正弦高度参考（默认高度上下 5 cm），可在运行时切换或重置，无需每次手改脚本。
- [ ] 图形界面：提供开始/停止控制，参数范围输入，实时曲线（目标高度 vs 实际高度，倾斜角度），显示失败状态。
- [ ] 自动调参循环：实现多轮试验（网格/随机/简单优化），失败时提前终止该轮；以选定指标挑选最佳运行。
- [ ] CSV 记录：每轮记录参数、指标、失败标记；单独保存“最佳”运行的参数与指标。
- [ ] 验证：短程试跑验证 GUI 控件、失败检测、日志写入；处理无图形/沙盒下的兼容性。
